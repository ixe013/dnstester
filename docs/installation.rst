============
Installation
============

At the command line::

    $ easy_install dnstester

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dnstester
    $ pip install dnstester
